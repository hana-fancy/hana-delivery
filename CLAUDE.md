# ファンシーフラワー 花束納品・加工在庫管理アプリ

山形県の花屋「ファンシーフラワー」のスーパー卸事業（花束の委託販売）を管理する社内ツール。
納品・返品・棚卸・加工・廃棄・売上（3社分）をデジタル化し、将来の推定在庫・仕入サジェストに繋げるのが最終目標。

詳細な仕様・運用ルールの一次情報は **`hana-handover_v9.md`**（引き継ぎドキュメント）。
迷ったら実装より先にこのファイルを読むこと。特にセクション9（推定在庫モデル）・10（開発運用注意）は実装前に必読。
このCLAUDE.mdは「今どこにいて、次に何をするか」の要約。詳細な過去の意思決定・議論の経緯はhandoverの方が正確。

## システム全体の位置づけ

このリポジトリは、ファンシーフラワー全社の経営ダッシュボード開発における**スーパー卸アプリ層（Layer 3）**を担う。

全体構想・共通仕様・他アプリとの連携方針は **`fancy_flower_経営ダッシュボード構想_v2_2.md`**（ハブMD）を参照すること。
→ このリポジトリに含めていない場合は、オーナー（ファンシーフラワー）から取得する。

**意思決定の分担：**
- 全体設計・方針判断 → 上位Coworkチャット（ハブMDを管理するチャット）
- 実装 → このClaude Code環境
- 実装中に「他アプリや共通基盤への影響がある判断」が生じたら、実装を止めて上位チャットに確認する

## 技術構成

- フロントエンド: HTML/CSS/JavaScript の単一ファイル（フレームワークなし、ビルドなし）
- ホスティング: GitHub Pages
- DB: Supabase（PostgreSQL）。`@supabase/supabase-js` は不使用、`fetch` + apikey/Authorizationヘッダーで直接REST API呼び出し（`sbFetch`関数、両アプリ共通）
- 認証: スタッフ名選択式（簡易版、PINコード認証は未実装）

## ファイル構成

| ファイル | 内容 | 行数目安 |
|---|---|---|
| `hana-delivery-app.html` | 花束納品管理アプリ（納品・返品・棚卸・売上取込3社・売上集計・カレンダー） | 約3,900行 |
| `hana-processing-app.html` | 加工・在庫管理アプリ（加工記録・廃棄記録・棚卸・集計・カレンダー） | 約1,300行 |
| `hana-handover_v9.md` | 詳細仕様書（一次情報源） | — |
| `dashboard.html` | 経営ダッシュボード（Layer 4 seed）。分析タブ＋予定タブ＝加工計画、ともに本番稼働中 | 約1,600行 |

両アプリは同一リポジトリで管理し、`MASTER_PRODUCTS`（商品マスタ）と`staff`テーブル（スタッフマスタ）をSupabase側で共通化している。**商品を追加・変更する場合は両HTML内の`MASTER_PRODUCTS`定義（1箇所ずつ、要同期）を編集。**

現状は単一ファイルのままJS/CSS分割はしていない（handoverセクション8で言及される「将来js/css分割」は未着手）。

## 現在地（2026年9月4日時点）

- 花束納品アプリ：骨格実装完了＋Claude Code移行後の最優先課題だった**推定在庫表示を実装・本番反映済み**（詳細は下記）
- 加工・在庫アプリ：主要機能実装完了、テスト運用中。**まだGitHub Pagesに未アップロード**
- 売上取込：もんとあーる・主婦の店・アオキの3社すべて実装完了・稼働中
- 売上集計・店頭販売実数入力・ファンシー自社店舗・納品フロー刷新：すべて実装完了
- ファンシーフラワー自社店舗（fancy_honten / fancy_esumall / fancy_pal）：companiesとstoresに追加済み
- **経営ダッシュボード dashboard.html：分析タブ・予定タブ（加工計画）ともに本番稼働中**（詳細は下記。2026-09-03セッションで大幅拡張、2026-09-04セッションでオーナーによるローカル検証完了・修正反映のうえpush済み）
- **もんとあーる税抜対応を実装・本番反映済み**（commit 0447c5b・push済み）：`hana-delivery-app.html` の `parseMontourEmail` が、`MONTOUR_TAX_FIX_DATE='2026-09-01'` 以降の売上を `sales_items` へ **税抜（÷1.10・標準税率10%）で保存**。商品判別キー（`MONTOUR_PRICE_MAP`/`MONTOUR_SPOT_MAP`）と `raw_label` はメール表記（税込）のまま。既存データ（〜2026-08-31）は無変換で保持。`montourUnitPriceForDb()` で単価変換→`amount=単価×数量`。dashboard.html は同名定数で 8/31以前の montour を読取時に÷1.10補正（相補・二重補正なし）

### 推定在庫機能（2026年8月16日実装・コミット77bd940）

`hana-delivery-app.html`の「記録」タブに`📊 推定在庫（本日）`セクションを追加。会社/店舗フィルターで店舗を絞り込むと、その店舗の今日時点の推定在庫が表形式・カード形式どちらでも表示される（フィルター未選択時は案内メッセージのみ）。

- **対象品目**：仏束花6価格帯（450/600/1000/1500/2000/3000円）＋キープフラワー（小袋/200ml）。`EST_INVENTORY_PRODUCTS`定数（`BUTSU_PRODUCTS` + `keep*`）
- **列**：推定在庫（今日開始時点）／返品（本日分）／納品（本日分）／在庫棚卸（本日実施していれば実数、なければ「—」）
- **計算**：直近の棚卸日（今日より前）を起点に、翌日〜昨日分の納品・返品・売上を日別に積算。積算途中で0以下になった日があればその日を新しい起点として0からリセットし継続（`computeStoreToday`関数）
- **重要な設計判断（未検証・実運用で要確認）**：棚卸を記録したその日自体の納品・返品・売上は「その日の棚卸カウントに反映済み」とみなし積算から除外し、**翌日から**積算している。もし実際の運用（訪問順序や売上発生タイミング）とズレがあれば`computeStoreToday`内の日付境界（`gt.${baseDate}`の起点）を調整する
- 棚卸が一度もない店舗は「不明」表示（本日分の納品・返品のみ表示）
- キープフラワー以外のカジュアル・スポット・値下げ品は「その他品目」として本日分の納品・返品のみ表示（推定在庫なし、価格帯別グルーピングはしていない）
- 全社・全店（未フィルター）時は計算負荷回避のため非表示（案内メッセージのみ）。会社のみ選択時はその会社の全店舗を並べて表示

### 経営ダッシュボード dashboard.html（2026年9月4日時点・push済み・本番反映）

2026-09-03セッションで予定タブを大幅拡張し、2026-09-04セッションでオーナーが実データを入れながらローカル検証。見つかった問題を都度修正し、検証OKのためpush済み。

**分析タブ（本番稼働中・今セッションで一部改修）**
- 商品別 Q・PQ・MQ の物日別集計。実績レイヤーと施策後（想定）レイヤーを分離。19列＋詳細列トグル。シナリオ保存（`dashboard_scenarios`）
- **期間セレクタを「年 × 物日」の2層に変更**（今セッション）。既存6期間（`KIRYUUBI_MASTER`）はid・日付を変えずに`event`/`year`タグを付与。既存シナリオ「神子」（`period_id=obon2026`）は年2026・物日8月盆で従来どおり読める
- マスタに無い年×物日は暦日ルール（`KIRYUUBI_EVENTS`＋`resolveKiryuubi`）から自動合成、`period_id`は`{物日}_{年}`形式
- 年リストは今年基準6年固定（過去4＋今年＋翌年）。データ有無では絞らない
- **V計算式は加算式**：`V1 = P×(1−粗利率)` / `V2 = P×委託手数料率` / `実V = V1+V2` / `M = P−実V` / `MQ = M×Q`。委託手数料は変動費としてMQから控除済み（MQ = PQ×0.4 は 粗利率60%・手数料20% での仕様どおりの値。M列と想定原価列が同値に見えるのは `1−0.6 = 0.6−0.2` の偶然でバグではない）
- 委託手数料率は現状**全社一律20%**（低手数料店＝エスモール12/本店0 のMQは過小）。基礎設定は`dashboard_settings`（Supabase）で全端末共通

**予定タブ＝加工計画（2026-09-04 ローカル検証完了・本番稼働中）**
- タブUI：`📊 分析 / 📅 予定`。予定タブは初回表示時に遅延ロード
- 物日マスタは分析タブと共通化（`KIRYUUBI_EVENTS`：春彼岸/母の日/7月盆/8月盆/9月彼岸/12月年末。各物日が集計ウィンドウ`start/end`と生産計画ウィンドウ`planStart/planEnd/peak`の両方を返す）。12月年末は正月1/3まで年跨ぎ。**彼岸の生産計画ウィンドウ末日は24日**（`higan_haru` planEnd=03-24、`higan_aki` planEnd=09-24＋2026オーバーライドも09-24。9/3セッションで09-26→09-24に短縮）
- 上部：年 × 物日種別 × 比較年（`年-1`/`年-2`、既定`年-1`）
- **計画対象＝卸3社（もんとあーる/主婦の店/アオキ）＋ファンシー本店**（`PLAN_COMPANIES`。本店は `company_id='fancy' AND store_id='fancy_honten'` で判定＝`planCoForRec`。エスモール店・パル店は対象外。9/3セッションで本店追加）
- グリッド：行＝仏束花6価格帯（定番）＋その他品目（既定は畳む・区切り行クリックで展開、`planShowExtras`）／列＝`plan_start`〜`plan_end`の各日／セル上段＝参照実績（卸3社＋本店 合算・読取専用）、下段＝加工計画入力（も/主/ア/本 の4系列）／**右端に固定2列**：「期間計」＝横計（参照計・会社別計）、「目標Q」＝補正ベースライン×係数の狙い値（緑）
- 色アラート（計画入力欄）：赤＝翌日の参照実績未満／橙＝翌々日実績×0.8未満／黄＝×0.9未満／青＝翌々日実績以上。**参照日→グリッド日はピーク日基準の日数オフセット**（ピーク列が必ず一致）。色ロジックは現状維持（ピーク前日が赤になる件はオーナー了承済み）
- 累積行（今年計画累積／参照年実績累積／差分。右端に期間総計も）＋ サマリーカード（計画合計／参照年合計／達成率／ピーク日時点の累積達成率）
- **仮置き生成パネル**：
  - 方式＝成長係数 or 目標MQ逆算（推定MQ据え置き→目標MQ入力→自動係数＝目標MQ÷推定MQ、全商品一律スケール）
  - 商品×会社の補正ベースライン表（去年の欠損データを手入力で補正）、係数%の**商品別（価格帯別）上書き**、「仏平均」で日カーブを仏束花合算に代用
  - **前倒し（リードタイム、既定3日）＋後方ならし（既定5日・フラット分散）** を日カーブに適用してピークを手前へ倒す（`genProductCurve`）。正規化してΣ=目標Q維持
  - 生成＝会社別`目標Q × 前倒しならしカーブ` → **商品×日の合計を50束、会社按分を10束に丸め**（端数は最大シェアの会社へ寄せ、商品合計＝50の倍数を維持）
  - 生成後メッセージに「計画合計・目標Q比・青/黄/橙/赤 日数」
  - **生成条件をシナリオに保存/復元**：`data.__genConfig`（jsonb内の予約キー、日付キーと非衝突）に mode/baseFactor/marginRate/targetMQ/baseline/factorOv/blend/lead/smooth/**note（生成メモ）** を同梱。旧シナリオ（`__genConfig`なし）は既定へ。関連：`currentGenConfig`/`applyGenConfig`/`resetGenAll`/`syncGenPanelInputs`/`renderPlanNoteDisplay`
  - **生成メモ**（`planGenNote`）：なぜこの係数・基礎数字にしたかを記録。グリッド上部にも常時表示。帯別「基準年」機能は入れない方針で決着（2023年はDBに無い＝半分手入力になる／毎年処方が変わる／係数と補正ベースラインで既に帯別調整可能。根拠の言語化は生成メモ＋外部の総括MDで持つ）
- シナリオ：`production_plan_scenarios`（新規テーブル・オーナーがSQL実行済み）に 名前＋メモ＋ref_year＋data(jsonb) で保存/呼び出し/削除
- 3桁の計画数でもセルが潰れないようCSS調整済み（`.pl-in` 幅拡大ほか）
- **前倒し均し（加工上限による分散）**：lead/smooth で部分的に対応。1日あたり加工上限を入れた明示的分散はまだ未実装＝次段の宿題
- 参考：`彼岸3ヶ年総括_目標設定の注意点_1.md`（2023-2025の彼岸振り返り。450/600円→2023基準×100-110%、1000円以上→2025基準×110-120%、ピークは中日前日＝前倒し配分、前半残業活用、加工済みロスと作成数をセット記録…）。オーナーがスタッフ・ミーティングのファシリテーション資料として作成。現状 `C:\Users\fancy\Downloads\` にあり要移動

**2026-09-04セッションでの検証結果・追加修正**（オーナーが秋彼岸2026の実データで仮置き→手入力を試しながら検証。「仮置きとしての機能は十分」との評価）
- **ファンシー全店集計の修正**：`PLAN_COMPANIES`の「フ」系列が`store_id='fancy_honten'`限定になっていたバグを修正。`company_id='fancy'`一致のみ（本店・エスモール店・パル店の3店舗合計）に変更し、ラベルを「本店」→「ファンシー」に改称（内部データキーは`fancy_honten`のまま維持・シナリオ互換）
- **前倒し（リードタイム）・ならし（後方分散）の商品別（価格帯別）上書き**を追加。`planGenLeadOv`/`planGenSmoothOv`。価格帯ごとに生鮮特性・需要先（墓/仏壇/洋花)・天候対応の必要性が異なるという業務知見に基づき、既定値を設定（`PLAN_GEN_LEAD_DEFAULT`/`PLAN_GEN_SMOOTH_DEFAULT`）：450円・600円＝前倒し1日/ならし6日（墓需要中心・総数多・天候対応優先）、1,000円＝前倒し2日/ならし6日（仏壇/墓の中間）、1,500円以上＝前倒し3日/ならし3日（洋花中心・日持ち重視・総数少）。空欄なら生成パネルのグローバル既定値にフォールバック。シナリオ保存にも同梱、旧シナリオはこの価格帯別既定値へフォールバック
- **日別・全品目の合計表示を追加**：各商品×日付セルに「計」（会社4系列の日合計）を表示、グリッド下部（累積行の上）に「全品目 日別計」行（非累積の日別グランド計）を追加
- **粗利（想定MQ）カードを追加**：「計画粗利（想定MQ）」「参照年粗利（想定MQ）」をサマリーカードに追加。仮置き生成の目標MQと同じ計算式（参照年平均単価×(基準粗利率−委託手数料率)＝単価M）を、計画/参照年それぞれの実数量に適用
- **Tab移動順のバグ修正**：入力欄で`e.preventDefault()`していなかったため、ブラウザ標準のTab移動（会社→会社の縦移動）とカスタム再フォーカスが競合し意図した横移動（同じ会社のまま日付方向）にならないバグを修正。Tab/Enterで確定→preventDefault→自前で再描画・再フォーカスする方式に変更
- **生成メモの入力欄を`<details>`（仮置き生成パネル）の外に移動**：デフォルト折りたたみのパネル内・奥まった位置にあり入力し忘れやすかったため、グリッド上部の常時表示エリアに直接編集可能な形で常設。旧・読み取り専用表示（`plan-note-display`）は廃止し統合
- **前倒し・ならしの説明tooltipを追加**：初めて使う人向けに、各入力欄にホバー説明と一文解説を追加
- コードレベルの変更のみで**未実施**：シナリオ保存→白紙化→呼び出しでの`leadOv`/`smoothOv`復元の実地確認（機構は実装済み・型的には他の生成条件と同じ経路で保存/復元される）

**新規Supabaseテーブル：`production_plan_scenarios`（実行済み）**
```sql
CREATE TABLE production_plan_scenarios (
  id uuid primary key default gen_random_uuid(),
  period_id text not null, scenario_name text not null,
  ref_year integer not null, memo text default '',
  data jsonb not null default '{}',
  created_at timestamptz default now(), updated_at timestamptz default now(),
  unique(period_id, scenario_name)
);
ALTER TABLE production_plan_scenarios DISABLE ROW LEVEL SECURITY;
```
data構造：`{ "2026-09-12": { "仏束花 450円": { "montour": 300, "shufu": 200, "aoki": 50 } } }`

**検証で判明した実データ事情**
- 秋彼岸2025は改装休業で**アオキが全商品ゼロ**（イズモ店頭・アオキ2店舗休業）→ 補正ベースライン手入力、または比較年2024を使う運用
- Supabaseの`sales_records`は 2024-08-01〜2026-08-20。「数年前」の実数はDB外＝補正表に手入力

**コードレビュー（3巡）で対応済みの主な修正**：母の日など可動物日のカーブずれ（ピーク日基準対応）／override反映漏れ／「空欄のみ埋める」の会社セル単位判定／12月年末の年跨ぎ／初期化順のmargin取り違え／並列fetch化 ほか
**据え置き（既知）**：セル編集ごとのグリッド全再描画（分析タブも同方式）／商品名の完全一致依存（`PLAN_ROW_PRODUCTS` が `sales_items.product_name` とバイト一致前提）

## 次にやること（優先順）

1. **経営ダッシュボード `dashboard.html` 予定タブ（加工計画）【本番稼働中・push済み】**
   - 2026-09-04セッションでオーナーがローカル検証を実施し、見つかった課題（ファンシー全店集計・前倒し/ならしの価格帯別化・日別合計表示・粗利カード・Tab移動順・生成メモ位置）はすべて修正・push済み（詳細は上の「現在地」参照）
   - **残作業**：前倒し均し（1日あたり加工上限を入れて超過分をピーク前の空き日へ分散）の明示実装。現状はlead/smooth（商品別上書き対応済み）で部分対応のみ、上限入力方式は未着手
   - 引き続き実運用しながらの微調整（市場入荷日＝月水金の水揚げ制約による会社別の前倒し調整は、今回追加したTab横移動で手作業運用にめどが立った。今後も使いながら気づいた点があれば都度対応）
   - シナリオ保存→白紙化→呼び出しでの`leadOv`/`smoothOv`（前倒し/ならしの商品別上書き）復元は、機構は実装済みだが実地確認はまだ。次回シナリオ機能を使う際に確認するとよい
   - 分析タブ v0.1（Q・PQ・MQ昨対・MQソート・V補正・シミュレーション・年×物日セレクタ）は実装済み・本番稼働中。**分析タブには9/3・9/4セッションとも手を加えていない**
2. **`彼岸3ヶ年総括_目標設定の注意点_1.md` を安定した場所へ移動**（現状 `C:\Users\fancy\Downloads\`）。予定タブの生成メモから根拠として名前参照する運用

3. **`processing_records`テーブルへのSQL未実行**（即実行可能）
   ```sql
   ALTER TABLE processing_records
     ADD COLUMN IF NOT EXISTS price integer DEFAULT 0,
     ADD COLUMN IF NOT EXISTS total_amount integer DEFAULT 0;
   ```

4. **推定在庫の実運用フィードバック反映**：「棚卸当日を積算から除外」という前提が実態と合っているか、実データで検証する

5. フィルターボタンの動的生成（現在`COMPANIES`から生成せずHTMLにハードコード。会社追加のたびに複数箇所を手動更新している）

6. 「値下げ品」専用入力エリア（追加商品エリアに5番目の区分として新設）

7. もんとあーるの季節限定品パーサー拡張（「産直花」以外のレシート表示への対応）

8. 加工・在庫アプリのGitHub Pagesアップロード

未実装項目の全リストはhandoverセクション「9. 残課題・ネクストアクション」参照（ただし推定在庫は上記の通り実装済み）。

## 実装時の必須ルール

- **日付はJST変換必須**。`toISOString()`はUTC返却。`todayISO()`パターン（`Date.getTime() + 9*60*60*1000`）に従うこと。UTC変換忘れは実際にバグを起こした実績あり（v9でのカレンダーロック判定バグ：`getHours()`→`getUTCHours()`修正）
- **Supabase通信は`sbFetch`のみ使用**。`supabase-js`は導入しない（v4で意図的に廃止済み）。**`dashboard.html`でも同じ`sbFetch`パターンを使うこと**
- **`sales_items`へのINSERT時**、パーサーが内部的に持つ`jan`（アオキ）や`needsReview`（主婦の店）等、テーブルに存在しない列をそのまま送るとPostgRESTがエラーを返す。保存直前に実列だけ明示的に抽出すること（v5で実際に発生した不具合）
- **`onclick`属性内のシングルクォート・テンプレートリテラル内のクォートでSyntaxErrorが起きた実績あり**。`document.createElement`ベースか文字列連結で回避すること
- 商品追加・変更は`MASTER_PRODUCTS`のみ編集（両HTMLに同じ定義があるので両方同期）
- スタッフの追加・変更はSupabase Table Editorで直接編集。HTML修正は不要

## 3社の売上取込パーサーの違い（handoverセクション7・9.6に詳細）

同じ「金額×季節」でも判別方式が全く異なるため、改修時は対象の会社のロジックを混同しないこと。

| 会社 | 判別方法 | 価格変更時の挙動 |
|---|---|---|
| もんとあーる | 金額表記（`MONTOUR_PRICE_MAP`/`MONTOUR_SPOT_MAP`） | メールの金額表記が変わる |
| 主婦の店 | レシート表示×季節（`SHUFU_PRODUCT_TABLE`） | 表示名は変わらず内部価格のみ変わる |
| アオキ | JANコード固定（`AOKI_JAN_MAP`） | 価格変更時はJANごと切り替わる（要人手更新） |

## GitHubへの反映

このリポジトリはGitHub Pagesで直接配信されている（`https://hana-fancy.github.io/hana-delivery/`）。`main`ブランチへのpushがそのまま本番反映（1〜2分）。破壊的な変更や動作未確認のコミットをpushしないこと。
