# ファンシーフラワー 花束納品・加工在庫管理アプリ

山形県の花屋「ファンシーフラワー」のスーパー卸事業（花束の委託販売）を管理する社内ツール。
納品・返品・棚卸・加工・廃棄・売上（3社分）をデジタル化し、将来の推定在庫・仕入サジェストに繋げるのが最終目標。

詳細な仕様・運用ルールの一次情報は **`hana-handover_v9.md`**（引き継ぎドキュメント）。
迷ったら実装より先にこのファイルを読むこと。特にセクション9（推定在庫モデル）・10（開発運用注意）は実装前に必読。
このCLAUDE.mdは「今どこにいて、次に何をするか」の要約。詳細な過去の意思決定・議論の経緯はhandoverの方が正確。

## システム全体の位置づけ

このリポジトリは、ファンシーフラワー全社の経営ダッシュボード開発における**スーパー卸アプリ層（Layer 3）**を担う。

全体構想・共通仕様・他アプリとの連携方針は **`fancy_flower_経営ダッシュボード構想_v2_2.md`**（ハブMD）を参照すること。
→ このリポジトリに含めていない場合は、オーナー（ファンシーフラワー）から取得する。

**意思決定の分担：**
- 全体設計・方針判断 → 上位Coworkチャット（ハブMDを管理するチャット）
- 実装 → このClaude Code環境
- 実装中に「他アプリや共通基盤への影響がある判断」が生じたら、実装を止めて上位チャットに確認する

## 技術構成

- フロントエンド: HTML/CSS/JavaScript の単一ファイル（フレームワークなし、ビルドなし）
- ホスティング: GitHub Pages
- DB: Supabase（PostgreSQL）。`@supabase/supabase-js` は不使用、`fetch` + apikey/Authorizationヘッダーで直接REST API呼び出し（`sbFetch`関数、両アプリ共通）
- 認証: スタッフ名選択式（簡易版、PINコード認証は未実装）

## ファイル構成

| ファイル | 内容 | 行数目安 |
|---|---|---|
| `hana-delivery-app.html` | 花束納品管理アプリ（納品・返品・棚卸・売上取込3社・売上集計・カレンダー） | 約3,900行 |
| `hana-processing-app.html` | 加工・在庫管理アプリ（加工記録・廃棄記録・棚卸・集計・カレンダー） | 約1,300行 |
| `hana-handover_v9.md` | 詳細仕様書（一次情報源） | — |
| `dashboard.html` | 経営ダッシュボード（Layer 4 seed）【開発中】 | — |

両アプリは同一リポジトリで管理し、`MASTER_PRODUCTS`（商品マスタ）と`staff`テーブル（スタッフマスタ）をSupabase側で共通化している。**商品を追加・変更する場合は両HTML内の`MASTER_PRODUCTS`定義（1箇所ずつ、要同期）を編集。**

現状は単一ファイルのままJS/CSS分割はしていない（handoverセクション8で言及される「将来js/css分割」は未着手）。

## 現在地（2026年8月28日時点）

- 花束納品アプリ：骨格実装完了＋Claude Code移行後の最優先課題だった**推定在庫表示を実装・本番反映済み**（詳細は下記）
- 加工・在庫アプリ：主要機能実装完了、テスト運用中。**まだGitHub Pagesに未アップロード**
- 売上取込：もんとあーる・主婦の店・アオキの3社すべて実装完了・稼働中
- 売上集計・店頭販売実数入力・ファンシー自社店舗・納品フロー刷新：すべて実装完了
- ファンシーフラワー自社店舗（fancy_honten / fancy_esumall / fancy_pal）：companiesとstoresに追加済み
- **経営ダッシュボード `dashboard.html` v0.1：機能実装は一通り完了、GitHub Pagesには未push（ローカルのみ）。シナリオ保存/呼び出し機能は実装済みだが未検証**（詳細は下記「経営ダッシュボード dashboard.html 進捗」）

### 推定在庫機能（2026年8月16日実装・コミット77bd940）

`hana-delivery-app.html`の「記録」タブに`📊 推定在庫（本日）`セクションを追加。会社/店舗フィルターで店舗を絞り込むと、その店舗の今日時点の推定在庫が表形式・カード形式どちらでも表示される（フィルター未選択時は案内メッセージのみ）。

- **対象品目**：仏束花6価格帯（450/600/1000/1500/2000/3000円）＋キープフラワー（小袋/200ml）。`EST_INVENTORY_PRODUCTS`定数（`BUTSU_PRODUCTS` + `keep*`）
- **列**：推定在庫（今日開始時点）／返品（本日分）／納品（本日分）／在庫棚卸（本日実施していれば実数、なければ「—」）
- **計算**：直近の棚卸日（今日より前）を起点に、翌日〜昨日分の納品・返品・売上を日別に積算。積算途中で0以下になった日があればその日を新しい起点として0からリセットし継続（`computeStoreToday`関数）
- **重要な設計判断（未検証・実運用で要確認）**：棚卸を記録したその日自体の納品・返品・売上は「その日の棚卸カウントに反映済み」とみなし積算から除外し、**翌日から**積算している。もし実際の運用（訪問順序や売上発生タイミング）とズレがあれば`computeStoreToday`内の日付境界（`gt.${baseDate}`の起点）を調整する
- 棚卸が一度もない店舗は「不明」表示（本日分の納品・返品のみ表示）
- キープフラワー以外のカジュアル・スポット・値下げ品は「その他品目」として本日分の納品・返品のみ表示（推定在庫なし、価格帯別グルーピングはしていない）
- 全社・全店（未フィルター）時は計算負荷回避のため非表示（案内メッセージのみ）。会社のみ選択時はその会社の全店舗を並べて表示

### 経営ダッシュボード dashboard.html 進捗（2026年8月28日実装）

新規単一HTMLファイルとして実装（`hana-delivery-app.html`/`hana-processing-app.html`は無改修）。同一Supabaseに`sbFetch`パターンで接続。**GitHub Pagesにはまだpushしていない（ローカルのみ）**。

**実装済み機能：**
- `sales_records`＋ネストした`sales_items`を1クエリで取得し、商品名単位でQ・PQ・Pを集計（商品×店舗の分解はまだしていない。委託手数料率は店舗により異なるが現状は全社一律の暫定値で近似、下記参照）
- 物日マスタ`KIRYUUBI_MASTER`（JS定数、年ごとに直接編集）。デフォルト期間は「今日までに開始日を迎えた中で一番新しいもの」を自動選択（例：8/6以降は当年のお盆が既定表示になる）
- V計算式（**ハブMD v2.0記載の掛け算式から加算式に修正済み。ハブMD側の更新が必要、下記「要共有」参照**）：
  ```
  V1（商品原価） = P × (1 - 基本粗利率)　※商品ごとに「実原価」を円で直接上書き可（差分ではなく実数入力）
  V2（委託手数料） = P × 委託手数料率
  実V = V1 + V2
  M = P - 実V、MQ = M × Q
  ```
- 基礎設定パネル：委託手数料率（全社一律・暫定、デフォルト20%＝もんとあーる/主婦の店の実値かつ最大値なので保守的）、基本粗利率（全社一括・暫定、デフォルト66%）。どちらもlocalStorage保存（ブラウザ・端末ごと）。商品ごとの個別入力があればそちらを優先
- 実店舗の委託手数料率の実際値（もんとあーる20%・主婦の店20%・アオキ18%・エスモール12%・パル16.5%・本店0%）は判明済みだが、店舗別ブレークダウンは将来対応。現状は一律値のため、エスモール・パル・本店（特に本店は実際0%）のMQは実態より低く出る
- もんとあーるの売上取込（既存アプリ側）は税込金額をそのまま`sales_items.price/amount`に保存している不具合を確認済み（他2社は税抜）。`MONTOUR_TAX_FIX_DATE`（2026-09-01）より前のもんとあーるデータは、dashboard.html集計時に÷1.10して税抜補正する形で暫定対応済み。**既存アプリ側（`hana-delivery-app.html`の`MONTOUR_PRICE_MAP`まわり）の恒久修正はオーナー承認済み・9/1切替で合意しているが、コードはまだ未着手**（次回対応）
- 商品区分（スタンダード→キープフラワー→値下げ品→カジュアル→季節品の順、区分内は価格昇順）が既定の並び。Q/PQ/MQ列ヘッダークリックでその列のフラット降順/昇順ソートに切替可（「既定の並びに戻す」リンクあり）
- シミュレーション：商品ごとに「追加Q個」「追加V円」を入力（％入力は使わない設計：MG研修的に％は実数の変化量を見誤らせるため）。Qのみ／Vのみ／合計の3種のMQ増分を行ごとに即時表示
- サマリーカード：合計Q・合計PQ・合計MQ（実績）に加え、目標MQ（入力欄）・達成率（実績÷目標）・MQ増分合計（シミュレーション）・推定合計MQ（シミュレーション後、目標到達で緑／未達で赤）
- **シナリオ機能**：ページを開く・期間を切り替えるたびに常に白紙（基本粗利率・実原価・追加Q・追加V・目標MQすべて空欄／既定値）からスタートする設計。商品ごとの入力値は自動保存しない。「名前を付けて保存」で現在の入力一式（各商品の基本粗利率・実原価・追加Q・追加V＋目標MQ）を期間ごとにSupabase`dashboard_scenarios`テーブルへJSONBで保存し、後から呼び出し／削除できる（複数人が別々の仮説「Aさん案」「Bさん案」を持ち寄って比較する用途を想定）。**⚠️このシナリオ保存・呼び出し・削除の一連の動作はまだユーザー側で検証できていない（次回再開時に最優先で確認）**

**Supabaseスキーマ変更（今回追加・実行済み）：**
```sql
-- 使わなくなった旧テーブル（実害なし、放置でOK）
-- dashboard_v_settings（product_name単位の自動保存を試みたが、シナリオ方式に変更したため不使用に）

CREATE TABLE dashboard_scenarios (
  id uuid primary key default gen_random_uuid(),
  period_id text not null,
  scenario_name text not null,
  target_mq integer,
  data jsonb not null default '{}'::jsonb,
  created_at timestamptz default now(),
  updated_at timestamptz default now(),
  unique(period_id, scenario_name)
);
-- RLSはRun without RLSで無効化（他テーブルと同じ運用）
```

**要共有（上位チャット・ハブMD更新が必要）：**
- ハブMD（`fancy_flower_経営ダッシュボード構想_v2_0.md`）セクション3「V（変動費・花材原価）の計算方針」の掛け算式（`基本V = P × (1-基本粗利率) × (1-委託手数料率)`）は、オーナー確認の結果、実際は上記の加算式が正しいことが判明。ハブMD側の更新が必要
- 店舗別の実委託手数料率（もんとあーる20%・主婦の店20%・アオキ18%・エスモール12%・パル16.5%・本店0%）は、将来「商品×店舗」の粒度に分解する際にそのまま使える値としてハブMDにも記録しておくとよい

## 次にやること（優先順）

1. **経営ダッシュボード `dashboard.html` の残作業【最優先】**
   - **シナリオ保存・呼び出し・削除機能の動作検証**（実装済みだがユーザー未検証。次回再開時に最初にここを確認する）
   - 検証OKならGitHub Pagesへpushするか判断（現状ローカルのみ、まだpushしていない）
   - 詳細・設計経緯は上記「経営ダッシュボード dashboard.html 進捗」参照

2. **もんとあーるの税込混入バグの恒久修正（`hana-delivery-app.html`、例外的に修正対象）**
   - もんとあーるの売上取込が`MONTOUR_PRICE_MAP`まわりで税込金額をそのまま`sales_items`に保存している不具合。オーナー承認済み・9/1切替で合意
   - 判別キー（メール記載の税込金額でマッチング）は変えず、DBに書き込む`price`/`amount`だけ税抜値に差し替える狭いスコープの修正
   - 本番の売上取込ロジックのため、修正後は必ずテストしてからpushする
   - dashboard.html側は`MONTOUR_TAX_FIX_DATE`（2026-09-01）を境に暫定補正済みなので、既存アプリ側の修正日とこの定数の整合を取ること

3. ハブMD（`fancy_flower_経営ダッシュボード構想_v2_2.md`想定）のV計算式セクションの更新を上位チャットに共有（掛け算式→加算式への修正、詳細は上記進捗欄）

4. **`processing_records`テーブルへのSQL未実行**（即実行可能）
   ```sql
   ALTER TABLE processing_records
     ADD COLUMN IF NOT EXISTS price integer DEFAULT 0,
     ADD COLUMN IF NOT EXISTS total_amount integer DEFAULT 0;
   ```

5. **推定在庫の実運用フィードバック反映**：「棚卸当日を積算から除外」という前提が実態と合っているか、実データで検証する

6. フィルターボタンの動的生成（現在`COMPANIES`から生成せずHTMLにハードコード。会社追加のたびに複数箇所を手動更新している）

7. 「値下げ品」専用入力エリア（追加商品エリアに5番目の区分として新設）

8. もんとあーるの季節限定品パーサー拡張（「産直花」以外のレシート表示への対応）

9. 加工・在庫アプリのGitHub Pagesアップロード

未実装項目の全リストはhandoverセクション「9. 残課題・ネクストアクション」参照（ただし推定在庫は上記の通り実装済み）。

## 実装時の必須ルール

- **日付はJST変換必須**。`toISOString()`はUTC返却。`todayISO()`パターン（`Date.getTime() + 9*60*60*1000`）に従うこと。UTC変換忘れは実際にバグを起こした実績あり（v9でのカレンダーロック判定バグ：`getHours()`→`getUTCHours()`修正）
- **Supabase通信は`sbFetch`のみ使用**。`supabase-js`は導入しない（v4で意図的に廃止済み）。**`dashboard.html`でも同じ`sbFetch`パターンを使うこと**
- **`sales_items`へのINSERT時**、パーサーが内部的に持つ`jan`（アオキ）や`needsReview`（主婦の店）等、テーブルに存在しない列をそのまま送るとPostgRESTがエラーを返す。保存直前に実列だけ明示的に抽出すること（v5で実際に発生した不具合）
- **`onclick`属性内のシングルクォート・テンプレートリテラル内のクォートでSyntaxErrorが起きた実績あり**。`document.createElement`ベースか文字列連結で回避すること
- 商品追加・変更は`MASTER_PRODUCTS`のみ編集（両HTMLに同じ定義があるので両方同期）
- スタッフの追加・変更はSupabase Table Editorで直接編集。HTML修正は不要

## 3社の売上取込パーサーの違い（handoverセクション7・9.6に詳細）

同じ「金額×季節」でも判別方式が全く異なるため、改修時は対象の会社のロジックを混同しないこと。

| 会社 | 判別方法 | 価格変更時の挙動 |
|---|---|---|
| もんとあーる | 金額表記（`MONTOUR_PRICE_MAP`/`MONTOUR_SPOT_MAP`） | メールの金額表記が変わる |
| 主婦の店 | レシート表示×季節（`SHUFU_PRODUCT_TABLE`） | 表示名は変わらず内部価格のみ変わる |
| アオキ | JANコード固定（`AOKI_JAN_MAP`） | 価格変更時はJANごと切り替わる（要人手更新） |

## GitHubへの反映

このリポジトリはGitHub Pagesで直接配信されている（`https://hana-fancy.github.io/hana-delivery/`）。`main`ブランチへのpushがそのまま本番反映（1〜2分）。破壊的な変更や動作未確認のコミットをpushしないこと。
