# ファンシーフラワー 開発引き継ぎドキュメント
作成日：2026年5月29日　最終更新：2026年8月3日　バージョン：v9

---

## 1. プロジェクト概要

### 事業背景
山形県の花屋「ファンシーフラワー」が運営するスーパー卸事業の管理効率化プロジェクト。
スーパーへの生花委託販売において、納品・加工・在庫データをデジタル化し、将来の仕入サジェストや経営改善に活用することが最終目標。

### 現在地（2026年8月3日時点）
- **花束納品管理アプリ**：骨格実装ほぼ完了。Claude Code移行フェーズへ移行準備中
- 直近の追加実装：もんとあーる季節品パーサー拡張・マルホン手動入力・各種バグ修正・売上集計UI改善
- **加工・在庫管理アプリ**：主要機能実装完了、テスト運用中
- **共通基盤**：スタッフマスタ・商品マスタをSupabaseで一元管理、Supabase接続方式を両アプリで`sbFetch`に統一済み
- **売上取込機能**：もんとあーる・主婦の店・アオキの3社すべて実装完了・稼働中
- **売上集計機能**：日別表示・期間合計表示・全フィルター組み合わせ印刷まで実装完了
- **店頭販売実販売数入力**：カレンダー画面・入力画面・修正フロー・売上集計統合まで実装完了

---

## 2. 技術構成

| 項目 | 内容 |
|------|------|
| フロントエンド | HTML/CSS/JavaScript（単一ファイル） |
| ホスティング | GitHub Pages |
| データベース | Supabase（PostgreSQL） |
| 認証 | スタッフ名選択式（現在はセキュリティ簡易版） |
| 対応端末 | スマホ（Android/iPhone）ブラウザ対応 |

### アクセスURL
```
花束納品管理アプリ：https://hana-fancy.github.io/hana-delivery/hana-delivery-app.html
加工・在庫管理アプリ：GitHub Pages に要アップロード（未実施）
```

### GitHubリポジトリ
```
https://github.com/hana-fancy/hana-delivery
```

### Supabase接続情報
```
Project URL: https://vvyzwwoduaqqamjvoqrp.supabase.co
anon key: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InZ2eXp3d29kdWFxcWFtanZvcXJwIiwicm9sZSI6ImFub24iLCJpYXQiOjE3Nzk4MTk1MzMsImV4cCI6MjA5NTM5NTUzM30.aKE-ZUjXJ0j3iBMQP1GLpfwwzPWGJFglhJa0vB52Q-U
```

---

## 3. 統合商品マスタ（両アプリ共通）

v3より両アプリが共通の `MASTER_PRODUCTS` を持ち、フラグで各画面の表示を制御する設計に変更。

```javascript
const MASTER_PRODUCTS = [
  {id:'butsu450',  name:'仏束花 450円',              price:450,  use_process:true,  use_waste:true,  use_inventory:true,  use_delivery:true,  season:false},
  {id:'butsu600',  name:'仏束花 600円',              price:600,  use_process:true,  use_waste:true,  use_inventory:true,  use_delivery:true,  season:false},
  {id:'butsu1000', name:'仏束花 1,000円',            price:1000, use_process:true,  use_waste:true,  use_inventory:true,  use_delivery:true,  season:false},
  {id:'wrap1000',  name:'ラップダルマ用束花 1,000円', price:1000, use_process:true,  use_waste:true,  use_inventory:true,  use_delivery:false, season:false},
  {id:'keep120',   name:'キープフラワー 小袋',        price:120,  use_process:false, use_waste:false, use_inventory:true,  use_delivery:true,  season:false},
  {id:'keep495',   name:'キープフラワー 200ml',       price:495,  use_process:false, use_waste:false, use_inventory:true,  use_delivery:true,  season:false},
  {id:'butsu1500', name:'仏束花 1,500円',            price:1500, use_process:true,  use_waste:true,  use_inventory:true,  use_delivery:true,  season:true},
  {id:'butsu2000', name:'仏束花 2,000円',            price:2000, use_process:true,  use_waste:true,  use_inventory:true,  use_delivery:true,  season:true},
  {id:'butsu3000', name:'仏束花 3,000円',            price:3000, use_process:true,  use_waste:true,  use_inventory:true,  use_delivery:true,  season:true},
];
```

### フラグの意味

| フラグ | 意味 |
|--------|------|
| `use_process` | 加工記録の定番・繁忙期商品に表示 |
| `use_waste` | 廃棄記録に表示 |
| `use_inventory` | 棚卸記録に表示 |
| `use_delivery` | 花束納品管理アプリの定番商品に表示 |
| `season` | 繁忙期商品フラグ（trueのものは追加商品セクションに表示） |

### 各アプリでの使われ方

```javascript
// 加工アプリ
PRODUCTS_STANDARD    = MASTER_PRODUCTS.filter(p => p.use_process && !p.season)   // 定番ボタン
PRODUCTS_SEASON_LIST = MASTER_PRODUCTS.filter(p => p.season)                      // 繁忙期セレクト
ALL_PRODUCTS         = MASTER_PRODUCTS.filter(p => p.use_inventory)               // 棚卸ピッカー

// 納品アプリ
STANDARD_PRODUCTS    = MASTER_PRODUCTS.filter(p => p.use_delivery && !p.season)  // 定番商品
```

> **商品を追加・変更する場合は `MASTER_PRODUCTS` の1箇所だけ編集すること。**

### 納品先（加工アプリ・処理_itemsで使用）

| ID | 店舗名 |
|----|--------|
| montour | もんとあーる |
| shufu | 主婦の店 |
| aoki | アオキ |
| barcode_none | バーコードなし |
| nakauroshi | 仲卸用 |

### 納品先会社・店舗（納品アプリ）

| 会社ID | 会社名 | 店舗ID | 店舗名 |
|--------|--------|--------|--------|
| montour | 産直もんとあーる | montour_hakusan | 白山店 |
| montour | 産直もんとあーる | montour_ekimae | 駅前店 |
| shufu | 主婦の店 | shufu_izmo | IZMO新斎町店 |
| shufu | 主婦の店 | shufu_mina | ミーナ店 |
| shufu | 主婦の店 | shufu_pal | パル店 |
| shufu | 主婦の店 | shufu_oyama | 大山店 |
| aoki | アオキ | aoki_mikawa | 三川店 |
| aoki | アオキ | aoki_esumall | エスモール店 |

※ アオキは数ヶ月後に茅原店が追加予定。

---

## 4. Supabaseテーブル構造

### staff（スタッフマスタ・両アプリ共通）

| カラム名 | 型 | 説明 |
|---------|-----|------|
| id | uuid | 主キー（自動生成） |
| no | integer | スタッフ番号 |
| full_name | text | 氏名 |
| display_name | text | アプリ表示名 |
| is_active | boolean | 有効フラグ |
| sort_order | integer | 表示順 |
| use_delivery | boolean | 納品アプリに表示するか |
| use_processing | boolean | 加工アプリに表示するか |

**登録済みスタッフ（no順）：**

| no | 氏名 | 表示名 | use_delivery | use_processing |
|----|------|--------|:---:|:---:|
| 0 | その他 | その他 | ✓ | ✓ |
| 1 | 後藤則子 | 社長 | - | - |
| 2 | 三浦和子 | 三浦 | - | - |
| 3 | 神子和義 | 神子 | - | - |
| 4 | 石川紗妃 | 紗妃 | - | - |
| 5 | 君ケ袋節 | 節 | - | - |
| 6 | 伊藤 楓 | 楓 | - | - |
| 7 | 佐藤誉範 | 誉範 | - | - |
| 8 | 酒井英明 | 酒井 | - | ✓ |
| 9 | 齋藤愛実 | 愛実 | - | ✓ |
| 10 | 瀬尾裕一 | 瀬尾 | ✓ | ✓ |
| 11 | 御橋拓海 | 御橋 | ✓ | ✓ |
| 12 | 佐藤　薫 | 薫 | ✓ | ✓ |
| 13 | 半田裕喜 | 半田 | ✓ | ✓ |
| 14 | 池田秀敏 | 池田 | ✓ | ✓ |
| 15〜25 | （その他スタッフ） | — | - | - |
| 26 | 菅原俊 | 俊 | - | ✓ |

スタッフの追加・変更：Supabase → Table Editor → staff テーブルで直接編集。HTMLは修正不要。

### delivery_records（納品記録）

| カラム名 | 型 | 説明 |
|---------|-----|------|
| id | uuid | 主キー |
| delivery_date | date | 納品日（JST） |
| delivered_at | timestamptz | 記録日時 |
| staff_name | text | 担当スタッフ名 |
| company_id / company_name | text | 会社ID・名 |
| store_id / store_name | text | 店舗ID・名 |
| memo | text | メモ |
| total_qty | integer | 合計点数 |
| total_amount | integer | 合計金額 |
| record_type | text | delivery / return / inventory |
| water_change | boolean | 水替え実施フラグ |
| is_edited | boolean | 修正済みフラグ |
| is_correction | boolean | 修正レコードフラグ |
| original_record_id | uuid | 元レコードID（修正時） |

### delivery_items（納品明細）

| カラム名 | 型 | 説明 |
|---------|-----|------|
| id | uuid | 主キー |
| delivery_record_id | uuid | delivery_recordsへの外部キー |
| product_name | text | 商品名 |
| price | integer | 単価 |
| quantity | integer | 数量 |
| amount | integer | 小計 |
| category | text | standard / season / casual / spot / inventory |

### processing_records（加工・廃棄・棚卸記録）

| カラム名 | 型 | 説明 |
|---------|-----|------|
| id | uuid | 主キー |
| record_date | date | 作業日 |
| recorded_at | timestamptz | 記録日時 |
| staff_name | text | 担当スタッフ名 |
| record_type | text | process / waste / inventory |
| product_name | text | 商品名 |
| product_id | text | 商品ID |
| price | integer | 販売単価（加工・廃棄のみ） |
| total_qty | integer | 合計数量 |
| total_amount | integer | 合計金額（price × total_qty） |
| memo | text | メモ（花材など） |

> **⚠️ Supabase未実行のSQL（要実行）：**
> ```sql
> ALTER TABLE processing_records
>   ADD COLUMN IF NOT EXISTS price integer DEFAULT 0,
>   ADD COLUMN IF NOT EXISTS total_amount integer DEFAULT 0;
> ```

### processing_items（加工先・数量明細）

| カラム名 | 型 | 説明 |
|---------|-----|------|
| id | uuid | 主キー |
| processing_record_id | uuid | processing_recordsへの外部キー（CASCADE DELETE） |
| product_name | text | 商品名 |
| product_id | text | 商品ID |
| destination_id | text | 納品先ID（加工記録のみ） |
| destination_label | text | 納品先名（加工記録のみ） |
| quantity | integer | 数量 |

**RLSポリシー：** 全テーブルにanon keyからの読み書き許可済み（社内ツールのため現状許容）。

### sales_records（売上記録メイン・v5で新規追加）

| カラム名 | 型 | 説明 |
|---------|-----|------|
| id | uuid | 主キー |
| sales_date | date | 売上の対象日 |
| received_at | timestamptz | 配信/取込時刻（NOT NULL。アオキは取込時刻を代用） |
| company_id / company_name | text | 会社ID・名 |
| store_id / store_name | text | 店舗ID・名 |
| source_type | text | montour / shufu / aoki |
| is_closed | boolean | 休業日フラグ（未実装：UI側のボタンは未着手） |
| total_qty / total_amount | integer | 合計点数・金額 |
| raw_filename | text | 元ファイル名（追跡用） |

### sales_items（売上明細・v5で新規追加）

| カラム名 | 型 | 説明 |
|---------|-----|------|
| id | uuid | 主キー |
| sales_record_id | uuid | sales_recordsへの外部キー（CASCADE DELETE） |
| product_name | text | 社内商品名（変換済み） |
| raw_label | text | 元のレシート/メール表記 |
| price / quantity / amount | integer | 単価・数量・小計 |
| category | text | standard/season/casual/spot/markdown/unused/unknown |

> ⚠️ **重要な実装上の注意**：`sales_items`へのINSERT時、パーサーが内部的に保持している`jan`（アオキ）や`needsReview`（主婦の店）等のプロパティをテーブルに存在しない列としてそのまま送ると、PostgRESTがエラーを返し保存が失敗する。**保存直前に必ずテーブルの実列だけを明示的に抽出してINSERTすること。**（v5で発生した実際の不具合・修正済み）

---

## 5. 加工・在庫管理アプリ（hana-processing-app.html）

### 実装済み機能一覧

#### ログイン・ホーム
- Supabaseの `staff` テーブルから `use_processing=true` のスタッフを取得してボタン表示
- 作業日表示・変更（デフォルト当日JST）
- DB接続ステータス表示

#### 加工記録入力
- **定番商品**：2列ボタングリッドでタップ追加（追加済みはグレーアウト）
- **追加商品（繁忙期）**：セレクトボックス → `PRODUCTS_SEASON_LIST` から動的生成
- **スポット商品**：商品名＋価格を自由入力して追加
- 各商品カードに納品先5行（もんとあーる・主婦の店・アオキ・バーコードなし・仲卸用）の数量入力
- 各カードに「計 〇束」「〇〇〇円」バッジをリアルタイム表示
- 保存ボタン下に「合計 〇束 / 〇〇〇円」サマリー表示

#### 廃棄記録入力
- 加工記録と同じ3セクション構成（定番・繁忙期・スポット）
- 単一数量入力（納品先内訳なし）
- 金額（廃棄ロス額）も記録・表示

#### 棚卸記録入力
- モーダルピッカーで商品選択
  - 通常商品（`use_process:true`）：一般リスト
  - 在庫管理品（`use_process:false`）：「在庫管理品」セクション（青ボーダー）
  - スポット品：自由入力
- 単一数量入力

#### 数量入力方式（全入力画面共通）
- ＋－ボタン（タップで1ずつ）
- 長押しで高速連続増減
- 数字部分タップで直接入力ダイアログ（Enterキー対応）

#### 本日の記録
- カード形式 / 表形式タブ切り替え
- 紫色バーで「本日の加工金額合計」と「廃棄金額」を常時表示
- カード形式：種別バッジ・担当者・商品名・合計束数・金額・納品先内訳・メモ
- 表形式：商品×加工/廃棄/棚卸のクロス集計

#### 集計
- タブ：当日 / 今週 / 今月 / 任意期間
- **表示順（上から）：**
  1. 加工金額合計カード（廃棄金額も小さく表示）
  2. 🌸 商品別 加工数・金額
  3. 🏪 納品先別 加工数（店舗タップで商品内訳を展開表示）
  4. 🗑 廃棄 商品別
  5. 👤 担当者別 加工数・金額

#### カレンダー
- 月カレンダー（前後ナビ）
- 各日に商品名＋数量チップ表示
- 日付タップで詳細シート（加工金額バー含む）

---

## 6. 花束納品管理アプリ（hana-delivery-app.html）

### 実装済み機能

#### 入力フロー
1. スタッフ選択（Supabase `use_delivery=true`）
2. 店舗選択（会社別アコーディオン）
3. 返品入力（必須・0も入力） ＋ 水替えチェック
4. 納品入力（定番・繁忙期・カジュアル・スポット）
5. 棚卸入力（任意）

#### 過去日付入力（v5で実装完了）
- メイン画面の日付表示をタップ → 日付ピッカーで任意の日付を選択（未来日は選択不可）
- カレンダー画面の日付セルをタップ → その日付でメイン画面に遷移
- 過去日付モード中は画面上部にオレンジのバナーで明示、「今日に戻る」ボタンで即時復帰
- `selectedDate`（nullなら今日）を `activeDate()` で参照し、返品・納品・棚卸の保存先日付に反映

#### 店舗修正機能（v5で実装完了）
- 記録画面の修正フローに「店舗変更」ボタンを追加（ヘッダー右上）
- タップで全店舗リストから選び直し可能、変更後はボタンが緑の「店舗変更 ✓」に変化
- 保存確認ダイアログに「三川店 → エスモール店に変更して保存」のように変更内容を明示
- 元レコード保持・修正レコード新規追加という既存の修正フロー思想は維持

#### スタッフ変更ボタンの移動（v5）
- ボトムナビ5タブ目が「変更」→「売上取込」に変更
- スタッフ変更はメイン画面ヘッダー右上の👤ボタンに移動

#### アラート
- 前回納品から3日以上：黄色警告 / 7日以上：赤色警告
- 前回水替えから5日以上：そろそろ水替え / 7日以上：水替えしてください

#### 記録・集計・カレンダー
- 週単位表示（◀▶移動・日付ジャンプ）
- 表形式 / カード形式切り替え、会社・店舗フィルター
- 修正ボタン（元記録保持・修正レコード新規追加、v5で店舗変更にも対応）
- 集計：当日/今週/今月/任意期間、品目×会社クロス集計
- カレンダー：月カレンダーに納品・水替え履歴、店舗ステータス一覧

### ⚠️ 未実装（次回着手予定）
- **納品入力フローの刷新**（店舗選択後にメニュー選択：返品を記録する／納品を記録する／棚卸を記録する／全部まとめて記録）← 次回最優先
- **ファンシー（自社）の店舗追加**（`COMPANIES`に`fancy`として本店・エスモール店・パル店を追加。束花の自社納品・実数入力を管理）
- 「記録」タブへの推定在庫・実数表示の追加（定番品のみ。詳細はセクション10.5参照）
- 「値下げ品」専用入力エリアの新設（追加商品エリアに5番目の区分として、元商品プルダウン＋時価入力。詳細はセクション10.3）
- もんとあーるの季節限定品（「産直花」以外のレシート表示）のメール解析対応

---

## 7. 売上取込機能（hana-delivery-app.html内に実装、v5で3社対応完了）

### 7.1 概要
ボトムナビ「売上取込」タブから、3社の売上ファイルをドラッグ&ドロップして自動解析・保存する機能。`sales_records`/`sales_items` テーブルに保存。

| 会社 | 入力形式 | 商品判別方法 | ステータス |
|---|---|---|---|
| もんとあーる | `.msg`（Outlookメール） | 金額判別 | ✅完了 |
| 主婦の店 | `.msg`（Outlookメール） | レシート表示×季節 | ✅完了 |
| アオキ | `.xlsx`（全国店舗データ） | JANコード固定 | ✅完了 |

### 7.2 使用ライブラリ（CDN・動的import）
```javascript
// .msgファイル解析（もんとあーる・主婦の店）
const mod = await import('https://esm.sh/@kenjiuno/msgreader@1.28.0');

// .xlsxファイル解析（アオキ）
const XLSX = await import('https://esm.sh/xlsx@0.18.5');
```
通常の `<script>` タグでは読み込めない（CommonJS形式のため）。動的 `import()` で esm.sh 経由でESM変換版を取得する方式。

### 7.3 もんとあーる：金額判別ロジック

メール本文の「産直花　金額×数量」を正規表現で抽出し、`MONTOUR_PRICE_MAP` で金額→商品名に変換。

| メール表記（税込） | 社内商品名 | 区分 |
|---|---|---|
| 121円 | キープフラワー 小袋 | standard |
| 495円 | 仏束花 450円 | standard |
| 545円 | キープフラワー 200ml | standard |
| 660円 | 仏束花 600円 | standard |
| 770円 | 仏束花1,000円（値下げ品） | **markdown** |
| 1,100円 | 仏束花 1,000円 | standard |
| 1,650円 | 仏束花 1,500円 | season |
| 2,200円 | 仏束花 2,000円 | season |
| 3,300円 | 仏束花 3,000円 | season |
| 上記以外の金額 | カジュアルフラワー（◯円） | **casual**（切り捨てず保持） |

> **値下げ品の運用ルール（確定）**：1,500→1,000円、2,000→1,500/1,000円、3,000→2,000/1,500円の値下げは、**定番金額と被るため売上データ上は判別不可**。売上取込では定番として計上（あいまい管理）。納品アプリ側（人が直接操作）では「値下げ品」として元の価格・新価格を明示的に記録できるようにする方針（セクション6の未実装項目、追加商品エリアに新区分として実装予定）。770円のみ被らないため`markdown`として区別。

> **季節限定品（門松・千両等）の運用ルール（確定）**：メールに来たものは全て自動記録する。季節判定（下記7.5と共通）により、盆→矢羽ススキ、正月→門松のように自動変換。季節外の時期に発生した場合はカジュアルとして記録（集計はメール表記のまま）。**現状の実装はまだ「産直花」パターンのみ抽出**であり、「門松」等の別レシート表示パターンの抽出は未実装（次回対応）。

店舗名マッチング：メールの『JA鶴岡もんとあ～る白山店』のような表記から、`COMPANIES`内の店舗名（「白山店」）が含まれるかで判定。

### 7.4 主婦の店：レシート表示×季節判別ロジック

メール本文の「商品名／数量点／金額円」の3行パターンを抽出し、`SHUFU_PRODUCT_TABLE`で「レシート表示×季節」→商品名に変換。

```javascript
const SHUFU_PRODUCT_TABLE = {
  'ファンシー束花1': {normal:'仏束花450円', ...全季節共通},
  'ファンシー束花2': {normal:'仏束花600円', ...全季節共通},
  'ファンシー束花3': {normal:'仏束花700円（値下げ品）', ...全季節共通, cat:'markdown'},
  '1,000円':{...全季節共通 standard}, '1,500円':{...season}, '2,000円':{...season},
  '季節1':{mothers:'仏束花2.5〜3,000円', bon:'同上', higan:'同上', newyear:'同上'},
  '季節2':{mothers:'アレンジ1500円', bon:'アジサイ', newyear:'千両'},
  '季節3':{mothers:'アレンジ2000円', bon:'矢羽ススキ', newyear:'南天'},
  '季節4':{mothers:'アレンジ3000円', bon:'ハス葉・花・実', newyear:'若松'},
  '花1':{normal:'カジュアル通常', mothers:'カジュアル', bon/higan/newyear:'カジュアル①'},
  '花2':{mothers:'STカーネ', bon:'ホオズキ', newyear:'門松'},
  '花3':{normal:'カジュアル予備1', mothers:'SPカーネ', bon/higan/newyear:'カジュアル②'},
  '花4':{normal:'サカキ', mothers:'プリザ・ソープ', bon:'シマススキ', newyear:'ユズリハ'},
  '注文分':'特注分', '鉢':{normal:'鉢物', bon:'段竹'}, 'アレンジ':{normal:'アレンジ汎用', bon:'ハスセット'},
  '薬その他':'キープフラワー小袋/200mlの混在（要分解、7.4.1参照）',
};
```
※ 対応表にない表記は切り捨てず、`category:'casual'` としてそのまま保持する。

**季節判定（4社共通ルールとして確定、もんとあーるにも適用）：**
```
4月15日〜5月15日   → mothers（母の日）
7月・8月            → bon（盆）
9月・3月            → higan（彼岸）
12月15日〜1月15日  → newyear（正月）
それ以外            → normal（通常）
```

**日付・配信時刻の取得方法（もんとあーると異なる）：**
メール本文に日時の明記がないため、①件名（例：「3月20日 合計売上実績メール」）から月日を抽出、②メールのメタデータ`messageDeliveryTime`（JST変換）から年と配信時刻を取得。22時の速報メールと翌日10時の確定メールは数値が同一のため、どちらを取込んでも同じ日付として認識・上書きされる。

**店舗名エイリアス：**メール本文の「新斎店」は店舗マスタの「IZMO新斎町店」と同一店舗。`SHUFU_STORE_ALIASES`で変換。

#### 7.4.1 「薬その他」の方程式分解
キープフラワー小袋(110円税抜)・200ml(495円税抜)が同一カテゴリ「薬その他」に混在。点数・金額から非負整数の方程式 `110x+495y=金額, x+y=点数` を解いて自動分解。

- 解が一意 → 自動で2品目に分解して保存
- 解が0個または複数 → 元の1明細のまま `needsReview:true` フラグを立てて「薬その他（要確認）」として保存（自動確定しない）

### 7.5 アオキ：JANコード固定判別ロジック

`.xlsx`ファイル（列構成：店舗番号/店舗名/JAN/品名/客数/点数/原価/売上、全国589店舗・約1,800行）から、店舗コード**1227（三川店）・1229（エスモール店）**の行のみ抽出し、JANコードで`AOKI_JAN_MAP`に変換。

```javascript
const AOKI_JAN_MAP = {
  '4573149819803':'仏束花450円', '4573149817458':'仏束花600円', '4573149817496':'仏束花1,000円',
  '4573149825880':'仏束花1,500円', '4573149825897':'仏束花2,000円',
  '4573149825903':'仏束花2,500円（未使用・予備）', // category:'unused'
  '4573149825910':'仏束花3,000円',
  // カジュアル各価格帯（花300〜花900、生花350〜480）→ category:'casual'
  '4573149826061':'キープフラワー小袋', '4573149826078':'キープフラワー200ml',
};
const AOKI_STORE_MAP = {'1227':{id:'aoki_mikawa',name:'三川店'}, '1229':{id:'aoki_esumall',name:'エスモール店'}};
```

- ファイル名（`20260319.xlsx`）から日付を自動検出
- アオキのメールには配信時刻がないため、**取込（アップロード）時刻**を`received_at`として代用
- アオキは「1日1ファイル確定・同日再アップロードは常に上書き」の運用のため、**ロック判定の対象外**（他社はロックあり）

> **アオキのJAN運用の特殊事情**：JANコードはアオキの全納品業者共通カタログから「価格の合うもの」を選んで使用する仕組み。キープフラワーのみファンシーフラワー専用に新規発行されたJAN。**価格変更時はJANごと切り替わる**（もんとあーるは金額表記が変わるだけ、主婦の店はJAN・表示名は変わらず内部価格のみ変わる、という3社で全く違う変更耐性を持つ。詳細は9.6参照）。

### 7.6 確認画面の表示マーク
| マーク | 意味 |
|---|---|
| 🏷 | 値下げ品（markdown） |
| 🌼 | カジュアルフラワー（casual） |
| ❓ | 未登録JAN（unknown） |
| 💤 | 未使用（unused、例：仏束花2,500円） |
| ⚠ | 要確認（needsReview、薬その他の分解不能ケース） |

### 7.7 同日複数回配信・ロック機構
| 会社 | 最終データの定義 | ロック判定 |
|---|---|---|
| もんとあーる | 19時配信 | あり（配信時刻比較） |
| 主婦の店 | 22時配信（翌日10時確定版と数値同一） | あり |
| アオキ | 1日1ファイル確定 | **なし（常に上書き）** |

`sales_date + store_id` が一致する既存レコードは削除→再INSERTで上書き。

### 7.8 売上集計表示機能（v6で実装完了）

集計タブに「売上」タブを追加。3社の売上データを日別・期間合計で可視化する。

#### 画面構成
- **期間選択**（開始日〜終了日）＋「表示」ボタン
- **日別 / 期間合計** の切り替えタブ
- **会社フィルター**（全社/もんとあーる/主婦の店/アオキ）→ 会社選択後に**店舗フィルター**が展開（2段階）
- **印刷/PDF出力**ボタン（ヘッダー右端に配置）

#### 日別表示（品目×日付クロス集計）
- 各セル：点数（上段）＋金額（下段）の2段表示
- 右端に期間合計列
- 品目のソート順：定番仏束花（価格昇順）→値下げ品→カジュアル（価格昇順）→キープフラワー（常に最後）
- 定番・カジュアル・キープフラワーの間に区切り行

#### 期間合計表示（品目×店舗クロス集計）
- データがある店舗のみ自動で列を生成
- 将来の仕入・加工サジェストへの接続点

#### 店頭店舗（v6で追加）
主婦の店の繁忙期スポット店舗3店を`COMPANIES`に追加済み：

| store_id | 店舗名 |
|---|---|
| shufu_izmo_gaiten | IZMO店店頭 |
| shufu_mina_gaiten | ミーナ店店頭 |
| shufu_oyama_gaiten | 大山店店頭 |

#### 印刷/PDF出力
ヘッダー右端に2ボタン配置（売上タブ表示時のみ表示）：
- **🖨️ 現在**：今見えている表だけを新しいタブで印刷
- **🖨️ 全ページ**：データがある全会社・全店舗の組み合わせを自動検出し、日別（全社→会社別→店舗別）＋期間合計（全社→会社別）を全て新しいタブで一括印刷。1フィルター＝1ページで改ページ

---

## 7.9 店頭販売 実販売数入力機能（v7で実装完了）

### 概要
繁忙期スポット店頭販売（IZMO店店頭・ミーナ店店頭・大山店店頭）の実販売数を手動入力する機能。`source_type:'gaiten'`として`sales_records`/`sales_items`に保存し、売上集計画面でそのまま参照できる。

### アクセス方法
売上取込タブ→「店頭販売」タブをタップ→カレンダー画面が開く

### カレンダー画面（screen-gaiten-cal）
- 入力済みの日に店舗名・点数バッジ（緑）を表示
- 日付セルをタップ→その日付で新規入力画面
- バッジをタップ→既存データを読み込んで修正入力画面
- 「＋入力」ボタン→今日の日付で新規入力
- `is_edited:true`のレコード（修正前の旧データ）はカレンダーに表示しない

### 入力画面（screen-gaiten）
| セクション | 内容 |
|---|---|
| 日付・店舗 | 日付選択（デフォルト今日）＋店頭3店舗から選択 |
| 定番品 | 仏束花450〜3,000円、`makeQtyControl`ベースでタップ直接入力対応 |
| 追加商品（季節品） | 日付から季節自動判定、`SHUFU_PRODUCT_TABLE`からプルダウン生成、時価入力欄付き |
| カジュアル | 価格＋メモ入力、`makeQtyControl`ベースでタップ直接入力対応 |

**集計ソート順**：定番仏束花（価格昇順）→値下げ品→カジュアル（価格昇順）→季節品→キープフラワー

### 修正フロー
納品修正と同じ思想：元レコードに`is_edited:true`をPATCH、修正レコードを`is_correction:true`＋`original_record_id`付きで新規INSERT

### 必要なDBカラム追加（実施済み）
```sql
ALTER TABLE sales_records
  ADD COLUMN IF NOT EXISTS is_edited boolean DEFAULT false,
  ADD COLUMN IF NOT EXISTS is_correction boolean DEFAULT false,
  ADD COLUMN IF NOT EXISTS original_record_id uuid REFERENCES sales_records(id);
```

### 重要な実装上の注意
- `gaitenQty`のキーは`gaiten_butsu450`（プレフィックス付き）。`getQtyStore`の`gaiten`モードでは`key:id`としてそのまま渡すこと（`replace('gaiten_','')`しない）
- `gaitenSeasonItems`は`_uid`（アンダースコア付き）を使う（他の配列と統一）

### 店舗マスタ（v7で更新）
- **大西店（`shufu_nishi`）を追加**：`closed:true`フラグ付き。閉鎖済みのため納品入力・修正画面の選択肢には非表示。売上取込パーサーは「大西店」表記でマッチして保存可能。集計フィルターでは「大西店(閉)」として表示。
- **店頭3店舗**：`shufu_izmo_gaiten`・`shufu_mina_gaiten`・`shufu_oyama_gaiten`（既存）

---

## 7.10 ファンシー自社店舗・納品フロー刷新（v8で実装完了）

### ファンシーフラワー（自社）店舗をCOMPANIESに追加

```javascript
{id:'fancy', name:'ファンシーフラワー', stores:[
  {id:'fancy_honten',  name:'本店'},
  {id:'fancy_esumall', name:'エスモール店'},
  {id:'fancy_pal',     name:'パル店'}
]}
```

- 納品入力・記録・集計・カレンダー・売上取込の全画面に自動反映
- 売上取込タブ「ファンシー」→カレンダー方式で実販売数を手動入力（店頭販売と同じUX）
- 保存時は`source_type:'fancy'`・`company_id:'fancy'`として記録

### 全画面のフィルター統一（ハードコード→fancy追加）

| 画面 | 追加したフィルター |
|---|---|
| 記録タブ（会社フィルター） | 「ファンシー」ボタン追加 |
| カレンダー画面（会社フィルター） | 「ファンシー」ボタン追加 |
| 売上集計（会社フィルター） | 「ファンシー」ボタン追加 |
| 売上取込タブ | 「ファンシー」タブ追加 |

> ⚠️ **注意**：現在これらのフィルターボタンはHTMLにハードコードされている。将来的に会社を追加する場合は各画面のHTMLとsetHistFilter・setSalesCompanyFilter・setCalFilter関数のリストも更新が必要。Claude Code移行後はCOMPANIESから動的生成するよう改善予定。

### 納品入力フローの刷新

店舗選択後に固定フロー（返品→納品→棚卸）だったものを、メニュー選択方式に変更。

```
店舗選択
    ↓
┌─────────────────────┐
│ ↩️ 返品を記録する    │ → 返品のみ保存→メニューに戻る
│ 📦 納品を記録する    │ → 納品のみ保存→完了画面
│ 📋 棚卸を記録する    │ → 棚卸のみ保存→完了画面
│ ✅ 全部まとめて記録  │ → 返品→納品→棚卸（従来通り）
└─────────────────────┘
```

**実装上の注意**：`menuMode`変数（`'all'|'return'|'delivery'|'inventory'`）でモードを管理。`submitReturn`・`submitDelivery`内でmenuModeを参照して遷移先を分岐。

---

## 7.11 v9での追加実装（2026年8月3日）

### もんとあーるパーサーの大幅拡張

**従来**：`産直花 金額×数量`のパターンのみ抽出

**変更後**：`商品名 金額×数量`の汎用パターンに拡張し、`MONTOUR_SPOT_MAP`で季節×レシート表示の2次元変換を実装。

```javascript
// 主婦の店と同じ2次元マップ構造
const MONTOUR_SPOT_MAP={
  '門松':    {bon:'矢羽ススキ', newyear:'門松'},
  '②松':    {bon:'アジサイ・段竹', newyear:'若松'},
  '南天':    {bon:'ホオズキ', newyear:'南天'},
  '千両':    {bon:'ホオズキの実', mothers:'プリザ・ソープ', newyear:'千両'},
  '花木':    {normal:'鉢物', mothers:'鉢物', bon:'シマススキ', higan:'鉢物', newyear:'花木'},
  'ハスの葉':{bon:'ハスの葉', newyear:'ユズリハ'},
  'ハスの花':{bon:'ハスの花・実'},
};
```

季節判定は`getShufuSeason(salesDate)`を共用（主婦の店と同じロジック）。

### アオキ手動入力（マルホン時代対応）

売上取込「アオキ」タブに「手動で実績を入力する」ボタンを追加。

- `source_type:'aoki'`・`store_id:'aoki_mikawa'or'aoki_esumall'`で保存
- `raw_filename:'マルホン時代（手動入力）'`で識別可能
- カレンダー方式（店頭販売・ファンシーと同じUX）

### 売上集計UI改善

- 期間選択に「昨日・今週・先週」ショートカットボタン追加（タップで自動セット→即表示）
- 日別表・期間合計表の合計列に金額（下段）を追加（点数＋金額の2段表示に統一）

### 閉鎖済み店舗の追加

| store_id | 店舗名 | 状態 |
|---|---|---|
| shufu_nishi | 大西店 | closed:true |
| shufu_voice | ボイス店 | closed:true |

### バグ修正

**カレンダーロック判定のUTC計算バグ**
`new Date().getHours()+9`→`new Date().getUTCHours()+9`に修正。JST環境のPCで二重変換が起きていた問題を解消。14時配信のメールが誤って「確定済み」と表示されていた。

---

## 8. 開発フェーズ移行（Claude Code移行）

### 移行の背景と判断

HTMLファイルが3,900行超になり、以下の問題が顕在化：
- 毎回全体を読み込むトークン消費が大きい
- 変更のたびにファイル全体を出力し直す無駄
- 推定在庫のような複雑なロジックの実装が困難

骨格機能（納品・返品・棚卸・売上取込・集計・店頭販売）がほぼ完成したこのタイミングで移行することで、残る推定在庫を確実に実装できる。

### 移行後の構成（予定）

```
hana-fancy/（GitHubリポジトリ）
├ hana-delivery-app.html  ← 現在の単一ファイル（分割予定）
├ hana-processing-app.html ← 加工在庫アプリ（同リポジトリで管理）
└ （将来）js/・css/等に分割
```

### Claude Code移行後の最優先実装

1. **推定在庫の表示**（記録タブへの統合）
   - 計算式：`直近棚卸残数 + 納品合計 - 返品合計 - 売上合計`
   - 定番品のみ（カジュアルは金額帯別あいまい集計）
   - 詳細設計はセクション10参照

2. **フィルターの動的生成**
   - 現在HTMLにハードコードされている会社フィルターボタンを`COMPANIES`から動的生成

---


## 9. 開発・運用上の注意事項

### 日付処理
`toISOString()` はUTC返却のためJST変換が必須。
```javascript
function todayISO(){
  const d = new Date();
  d.setTime(d.getTime() + 9*60*60*1000);
  return d.toISOString().slice(0, 10);
}
```

### Supabase fetch方式（両アプリ共通）
`@supabase/supabase-js` は不使用。fetch + apikey/Authorization ヘッダーで直接REST API呼び出し。
v4より花束納品管理アプリも同方式に統一済み。

```javascript
async function sbFetch(path, opts={}) {
  const res = await fetch(SUPABASE_URL + path, {
    ...opts,
    headers: {
      'apikey': SUPABASE_KEY,
      'Authorization': 'Bearer ' + SUPABASE_KEY,
      'Content-Type': 'application/json',
      'Prefer': 'return=representation',
      ...(opts.headers || {})
    }
  });
  if (!res.ok) throw new Error(await res.text());
  const txt = await res.text();
  return txt ? JSON.parse(txt) : [];
}
```

#### REST APIクエリ早見表

| 操作 | 例 |
|------|-----|
| SELECT（条件・ソート） | `/rest/v1/テーブル?カラム=eq.値&order=カラム.asc&select=*` |
| SELECT（複数条件IN） | `/rest/v1/テーブル?カラム=in.(値1,値2)` |
| SELECT（リレーション） | `/rest/v1/テーブル?select=*,子テーブル(*)` |
| INSERT | `method:'POST', body:JSON.stringify({...})` |
| UPDATE | `method:'PATCH', body:JSON.stringify({...}), pathに ?id=eq.XXX` |
| INSERT後にIDを取得 | `headers:{'Prefer':'return=representation'}` → 返り値の `[0]` を使用 |
| INSERT後にIDが不要 | `headers:{'Prefer':'return=minimal'}` |

### JS文字列エスケープ
`onclick` 属性内でのシングルクォートエスケープが `SyntaxError` になるケースあり。
テンプレートリテラル内のクォート問題も発生実績あり。
→ `document.createElement` ベース実装、または文字列連結で回避すること。

### GitHubへのアップデート手順
1. このチャットでHTMLファイルを修正・ダウンロード
2. GitHubの該当ファイルを削除
3. 新しいファイルをアップロード → Commit
4. 1〜2分後に反映

### Supabaseスキーマ変更履歴
| SQL | 内容 | 実施済み |
|-----|------|:---:|
| supabase-setup.sql | 初期テーブル作成 | ✓ |
| supabase-phase2.sql | record_typeカラム追加 | ✓ |
| supabase-phase3.sql | water_changeカラム追加 | ✓ |
| supabase-phase4.sql | is_edited等カラム追加 | ✓ |
| supabase-staff-master.sql | staffテーブル作成・全スタッフ登録 | ✓ |
| supabase-sales.sql | sales_records/sales_itemsテーブル作成（v5） | ✓ |
| processing_recordsにprice・total_amountカラム追加 | 下記SQL | **⚠️ 未実施** |

```sql
-- 未実施・要実行
ALTER TABLE processing_records
  ADD COLUMN IF NOT EXISTS price integer DEFAULT 0,
  ADD COLUMN IF NOT EXISTS total_amount integer DEFAULT 0;
```

---

## 9. 残課題・ネクストアクション

### 加工・在庫管理アプリ
- [ ] `processing_records` への `price` / `total_amount` カラム追加SQL実行（**最優先**）
- [ ] GitHub Pagesへのアップロード（hana-processing-app.htmlをリポジトリに追加）
- [ ] 実地テストフィードバックの収集と反映

### 花束納品管理アプリ（次フェーズ：Claude Code移行後）
- [ ] **推定在庫の表示**（「記録」タブへの統合、詳細はセクション10.5）← Claude Code移行後の最優先
- [ ] フィルターボタンのCOMPANIESからの動的生成（現在はHTMLにハードコード）
- [ ] **「記録」タブへの推定在庫・実数表示**（詳細はセクション10.5）
  - 定番品：推定在庫（前日終了時点）＋当日納品−返品＋棚卸入力時はそちらを優先表示
  - カジュアル・スポット：金額帯別のあいまい集計で表示。推定在庫は出さない。納品・売上の実数のみ表示
- [ ] 「値下げ品」専用入力エリアの新設（追加商品エリアに5番目の区分。元商品プルダウン＋値下げ後価格を入力。詳細はセクション10.3）
- [ ] もんとあーるの季節限定品（門松・千両等、「産直花」以外のレシート表示）のメール解析対応（正規表現の拡張が必要）
- [ ] 休業日フラグ（`is_closed`）の入力UI実装（テーブルのカラムは用意済みだが、ボタン等のUIは未着手）
- [ ] アオキ茅原店の追加
- [ ] PINコード認証の追加

### 両アプリ共通・将来
- [ ] 振り返りレポート機能（スタッフ自身が自分の納品・販売を振り返るための画面。経営者向け分析というより現場の気づき支援が主目的。ボリュームが大きいため独立アプリとして切り出す可能性も検討中）
- [ ] 繁忙期モード（お盆・お彼岸・年末）
- [ ] 仕入数サジェスト機能（在庫から推定した最低納品数を提示する仕組み。将来ロードマップの具体案の一つとしてイメージ図あり、実装は推定在庫機能の完成後）
- [ ] 本格認証（メール＋パスワード、権限管理）
- [ ] マージン・精算手数料の自動計算

---

## 10. 推定在庫モデル・「記録」タブ設計（v5、次回実装予定）

### 9.1 データの信頼性構造（確定済み・全商品共通）
```
推定在庫 = 直近の「在庫確定日」の残数
        ＋ それ以降の納品合計（参考値・数え間違いあり）
        − それ以降の返品合計（比較的正確）
        − それ以降の売上合計（最も確実・POSデータ）

「在庫確定日」は次のいずれか新しい方：
  ① 直近の棚卸入力日
  ② 直近で推定在庫が0以下になった日（自然リセット）
```
万引き・数え間違い等の誤差は「実在庫－推定在庫」の差異に吸収される。積算は直近の在庫確定日以降のみに限定するため、データが年単位で蓄積されても計算コストは増加しない。

門松等の季節限定品（広義のカジュアル）も「納品－返品－売上」の積算構造は定番品と同じロジックで成立することを確認済み（継ぎ足し運用がある商品でも同様）。

### 9.2 「記録」タブの表示イメージ（確定・実装待ち）

**定番品：**
```
仏束花 450円　推定在庫:5　納品:20　返品:5　在庫(棚卸):20
```
推定在庫・在庫（棚卸）はオーナー作成のイメージ画像で確認済み。棚卸が一度も入力されていない期間は「不明」と表示（無理な積算はしない）。

**カジュアル・スポット（大カテゴリ「カジュアル」の中の小カテゴリとして金額帯別にあいまい集計）：**
```
カジュアル 300円　納品:10　返品:0　（推定在庫は出さない）
カジュアル 400円　納品:10　返品:0
スポット   400円　納品:10　返品:0
スポット   500円　納品:10　返品:0
```
- 金額帯が行の単位（商品名ではない）。メールでは「産直花300円×10束」「門松400円×10束」のように金額で来るため、納品時の金額帯とそのまま対応させる
- 推定在庫を出さない理由：同じ金額帯でも日によって商品（門松/矢羽ススキ等）が変わるため、複数日にわたる積算に意味がなくなる。「あいまい集計」と「複数日積算」を組み合わせると誤差が拡大するため
- 納品数・販売数の実数のみ表示

### 9.3 「値下げ品」運用ルール（確定・UI未実装）
開花等で商品価値が下がった際の格下げ再納品。データ上は「元商品の返品」＋「値下げ品の納品」の2つの動きとして記録。

```
例）1,000円仏束花が売り場で開花
　→ 一旦返品（1,000円仏束花の返品として記録）
　→ カジュアル700円として再納品
```

値下げパターン一覧（もんとあーる運用が中心。主婦の店・アオキも将来発生し得る）：
| 元の商品 | 値下げ後 |
|---|---|
| 1,000円 | 700円 |
| 1,500円 | 1,000円 |
| 2,000円 | 1,500円 or 1,000円 |
| 3,000円 | 2,000円 or 1,500円 |

**売上取込（自動）と納品入力（人力）で精度を分ける設計：**
- 売上取込：金額のみのため判別不能 → 定番として計上（770円のみ被らないため markdown 区別、セクション7.3参照）
- 納品アプリ入力：人が直接操作するため正確に記録可能 → 追加商品エリアに「値下げ品」区分を新設し、元商品（プルダウン）＋値下げ後価格（自由入力）を選べるようにする（**未実装**）

### 9.4 季節限定品（スポット）運用ルール（確定・パーサー拡張待ち）
門松・千両・南天等、レシート表示名は固定だが中身が季節で変わる商品。

- **納品アプリ入力**：人が直接「スポット」欄に商品名を手動入力（既存仕様のまま）
- **売上取込（自動）**：季節判定ルール（7.4参照）で自動変換し`spot`または`casual`として記録。メールに来たものは全て記録する。季節が一致しない時期に発生した場合はカジュアルとして記録（集計はメール表記のまま）
- もんとあーるの「産直花」以外のレシート表示（門松等）をメール解析対象に含める実装が**未着手**（7.3参照）

### 9.5 「カジュアル」と「スポット」の関係（確定）
```
JANコード全体
├ 定番（仏束花450〜3,000円・キープフラワー）季節問わず固定
└ カジュアルフラワー（定番以外すべて。上位概念）
    ├ 通常のカジュアル（時価の生花束）
    └ スポット（季節限定品：門松・千両等）→カジュアルのサブカテゴリ
```
「カジュアル」と「スポット」は並列の区分ではなく、スポットはカジュアルの内訳。データ保存は分けるが、「記録」タブの表示は同じ大カテゴリ「カジュアル」の中で小カテゴリ別に並べる（9.2参照）。

主婦の店の「カジュアル①」「カジュアル②」のような複数JAN運用は、同価格帯でも品種（バラ系/かすみ草系等）を区別して記録するための仕組み。データ上は広義のカジュアルとして合算で問題ないが、メモ欄に品種を残す運用が将来の仕入分析に有用。

### 9.6 3社のJAN運用・価格変更耐性比較（確定）

| | JANと商品の対応 | 価格変更時の挙動 | システム側で更新すべき対応表 |
|---|---|---|---|
| もんとあーる | 1つのJANに複数商品（**金額で判別**） | JANは同じ、メールの金額表記が変わる | `MONTOUR_PRICE_MAP`（金額→商品名） |
| 主婦の店 | JANで一意に固定 | JAN・レシート表示名は同じ、**価格だけ**内部的に変わる | 更新不要（表示名が変わらないため） |
| アオキ | JANで一意に固定 | **JANごと**新しいものに切り替わる | `AOKI_JAN_MAP`（JANコード→商品名）に新JAN追加 |

主婦の店が最もシステム影響が少ない。アオキは価格変更時に必ず人間がJANマスタの更新作業を行う必要がある。

---

## 11. このドキュメントの使い方

**新しいチャットでこの開発を継続する場合**、このドキュメントを冒頭に貼り付けた上で作業内容を伝えることで、Claudeが同じ水準から開発を継続できます。

| チャットの種類 | 参照すべきセクション |
|---|---|
| 加工・在庫管理アプリの開発継続 | セクション2・3・4・5・8 |
| 花束納品管理アプリの開発継続（記録/集計タブ拡張） | セクション2・3・4・6・9・10 |
| 売上取込機能の開発継続（3社対応の改修・拡張） | セクション2・4・7・10 |
| 全体構想・次フェーズ計画 | セクション1・9・10

---

## 12. バージョン変更履歴

| バージョン | 更新日 | 主な変更内容 |
|-----------|--------|-------------|
| v1 | 2026年5月29日 | 初版作成（花束納品管理アプリのみ） |
| v2 | 2026年6月9日 | スタッフマスタをSupabaseで一元管理、加工アプリ開発開始 |
| v3 | 2026年6月9日 | 商品マスタ統合（MASTER_PRODUCTS）、加工アプリ主要機能完成 |
| v4 | 2026年6月12日 | 花束納品アプリのSupabase接続を `sbFetch` に統一（`supabase-js` 廃止）、実地テストフィードバック①（過去日付入力）を残課題に追加 |
| v5 | 2026年6月24日 | **過去日付入力・店舗修正機能を実装完了**。**売上取込機能を3社（もんとあーる・主婦の店・アオキ）すべて実装完了**（`sales_records`/`sales_items`テーブル新設）。推定在庫モデル・「記録」タブ表示設計・3社JANマスタの詳細仕様を全体構想チャットでの議論を反映して追記。値下げ品・季節限定品（スポット）の運用ルールを確定（UI実装は次回）。次回着手項目（記録/集計タブの拡張）を明確化 |
| v6 | 2026年7月17日 | **売上集計機能を実装完了**（日別・期間合計・2段階フィルター・全ページ印刷）。主婦の店スポット店舗3店追加。修正フロー改善（resolveLatestRecords・履歴ボタン）。表形式品目列固定。値下げ品入力UI新設。売上取込エラー対応（PGRST102修正） |
| v7 | 2026年7月25日 | **店頭販売実販売数入力機能を実装完了**（カレンダー・入力・修正）。大西店マスタ追加（closed:true）。売上集計ソート順確定。gaitenQtyキー不一致バグ修正。is_edited除外でカレンダー重複解消 |
| v8 | 2026年7月26日 | 納品入力フロー刷新（メニュー選択方式）。ファンシー自社店舗追加・全画面フィルター統一。次フェーズ：Claude Code移行 |
| v9 | 2026年8月3日 | **もんとあーるパーサー大幅拡張**（②松・門松等の季節品を季節×レシート表示の2次元マップで正確変換）。**アオキ手動入力**（マルホン時代対応、`raw_filename:'マルホン時代（手動入力）'`）。売上集計UI改善（ショートカットボタン・合計列金額追加）。ボイス店・大西店をclosed:trueで追加。カレンダーロック判定のUTC計算バグ修正。**次フェーズ：Claude Code移行→推定在庫実装** | **納品入力フロー刷新**（店舗選択後メニュー方式：返品のみ／納品のみ／棚卸のみ／全部まとめて）。**ファンシーフラワー自社店舗追加**（本店・エスモール店・パル店、`source_type:'fancy'`）。**全画面のファンシーフィルター統一**（記録・カレンダー・集計・売上取込）。次フェーズ：Claude Code移行→推定在庫実装 | **店頭販売実販売数入力機能を実装完了**（カレンダー画面・入力画面・修正フロー・売上集計統合）。**大西店（`shufu_nishi`）をマスタに追加**（`closed:true`、閉鎖済み・過去データ用）。売上取込カレンダーの点数を全店舗合計に修正。売上集計ソート順確定（カジュアル→季節品の順）。店頭販売数量入力のgaitenQtyキー不一致バグ修正。次回最優先：納品入力フロー刷新・ファンシー自社店舗追加 | **売上集計機能を実装完了**（集計タブに「売上」タブ追加）：日別表示（品目×日付）・期間合計表示（品目×店舗）・2段階フィルター（会社→店舗）・全フィルター組み合わせ印刷/PDF出力（🖨️現在/🖨️全ページの2ボタン）。**主婦の店の繁忙期スポット店舗3店を追加**（IZMO店店頭・ミーナ店店頭・大山店店頭）。**修正フローの改善**：修正の再修正対応・修正チェーン集計の正確化（resolveLatestRecords）・修正履歴ボタン追加。**表形式の品目列固定**（position:sticky）・値下げ品の商品名折り返し表示。**値下げ品入力UI新設**（納品・修正画面に専用エリア追加、1000/1500/2000/3000円が選択対象）。売上取込エラー対応（DELETEエラーの無視・エラー詳細表示）。売上集計ソート順確定（定番→値下げ品→カジュアル→キープフラワー）。次回最優先は売上取込画面の入力済み日カレンダー表示 |
